AWK=$(which awk)
BZIP2=$(which bzip2)
FILE=$(which file)
GZIP=$(which gzip)
# TODO: Get this automatically.
LIBNXZ="LD_PRELOAD=$(pwd)/libnxz.so.0 LD_LIBRARY_PATH=$(pwd) ./minigzipsh"
SED=$(which sed)
SHA256SUM=$(which sha256sum)
WGET=$(which wget)
XZ=$(which xz)
