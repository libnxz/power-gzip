# Not all distros use bash, but we do depend on bash here.
SHELL = /bin/bash

AWK = $(shell which awk)
GZIP = $(shell which gzip)
SED = $(shell which sed)
SHA256SUM = $(shell which sha256sum)
WGET = $(shell which wget)
